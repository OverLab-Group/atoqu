
Atoqu Index — v0.6

The internal index is a lightweight metadata file:

- data/index.aix stores:
  - document count
  - basic segment parameters

The actual vectors remain in VectorStore storage.

