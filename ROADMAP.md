
Roadmap — Atoqu Search Engine

Short-term

- Complete internal utils (net, sockets, string helpers)
- Refactor core to use atoqu::Json, atoqu::Log, atoqu::FileSystem
- Expand HTML docs (CLI, HTTP API, config, architecture)

Mid-term

- Real LLM embedding backend (external service or local model)
- Smarter HybridMode combining multiple signals
- Pluggable storage backends (file, SQLite, external Vector DB)
- Richer optimizer with per-user and per-session profiles

Long-term

- Full-featured search service with auth, multi-tenant indices
- Benchmarks vs existing search engines
- Dev.to series documenting Atoqu's evolution
- Plugin ecosystem for custom modes and ranking strategies
